Things to do
================================
- Add regression tests for new version of `stat_density_ridges` etc.
- Add separate styling options for vertical lines.
- In legend drawing code, can we detect the presence of jittered points from the input data, not from the parameter setting?

